name: Daily News

on:
  schedule:
    - cron: '30 22 * * *'
  workflow_dispatch:

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  run:
    runs-on: ubuntu-latest

    steps:
      - name: 코드 가져오기
        uses: actions/checkout@v5

      - name: Python 설치
        uses: actions/setup-python@v6
        with:
          python-version: '3.11'

      - name: 라이브러리 설치
        run: pip install requests deep-translator

      - name: 실행
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          NTFY_TOPIC: ${{ secrets.NTFY_TOPIC }}
        run: |
          echo "GEMINI_API_KEY exists: ${GEMINI_API_KEY:+yes}"
          echo "NTFY_TOPIC exists: ${NTFY_TOPIC:+yes}"
          python -u main.py
