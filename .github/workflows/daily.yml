name: Daily News

on:
  schedule:
    - cron: '30 22 * * *'  # 한국시간 오전 7:30
  workflow_dispatch:

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  run:
    runs-on: ubuntu-latest

    steps:
      - name: 코드 가져오기
        uses: actions/checkout@v5

      - name: Python 설치
        uses: actions/setup-python@v6
        with:
          python-version: '3.11'

      - name: 라이브러리 설치
        run: pip install requests

      - name: 실행
        env:
          OPENAI_API_KEY_JUNS: ${{ secrets.OPENAI_API_KEY_JUNS }}
          NTFY_TOPIC: ${{ secrets.NTFY_TOPIC }}
          ALPHAVANTAGE_API_KEY: ${{ secrets.ALPHAVANTAGE_API_KEY }}
        run: |
          echo "OPENAI_API_KEY_JUNS exists: ${OPENAI_API_KEY_JUNS:+yes}"
          echo "NTFY_TOPIC: $NTFY_TOPIC"
          echo "ALPHAVANTAGE_API_KEY exists: ${ALPHAVANTAGE_API_KEY:+yes}"
          python -u main.py
